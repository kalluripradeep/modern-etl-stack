apiVersion: v1
kind: Service
metadata:
  name: postgres-dest
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "etl-stack.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app: postgres-dest
  ports:
    - port: 5432
      targetPort: 5432

apiVersion: v1
kind: Service
metadata:
  name: kafka-connect
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "etl-stack.labels" . | nindent 4 }}
spec:
  selector:
    app: kafka-connect
  ports:
    - port: 8083
      targetPort: 8083

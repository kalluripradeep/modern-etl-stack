apiVersion: v1
kind: Service
metadata:
  name: postgres-source
  namespace: etl
spec:
  selector:
    app: postgres-source
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None   # headless — required for StatefulSet and CDC slot resolution

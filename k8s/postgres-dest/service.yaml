apiVersion: v1
kind: Service
metadata:
  name: postgres-dest
  namespace: etl
spec:
  selector:
    app: postgres-dest
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None   # headless — required for StatefulSet
